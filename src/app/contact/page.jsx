import ContactClient from "./ContactClient";

export const metadata = {
    title: "Contact Me",
    description: "If want cantact with me, please leave a message for me",
};

const Contact = () => {
    return <ContactClient />
};

export default Contact;
